# library

マインドマップ一覧・フォルダ選択などのエントリ画面を置くfeatureです。

ホーム画面のフォルダ選択ボタンはApplicationの`CreateMarkdownInFolder`を呼びます。Markdown一覧は`ListMindMapFiles`経由で取得し、`MindMapFileListPage`で表示します。SAFやDocument Pickerは呼びません。
