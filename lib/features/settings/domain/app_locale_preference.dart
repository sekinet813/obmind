/// User preference for app UI language. Not stored in Markdown.
enum AppLocalePreference {
  /// Follow the device language (with app fallback rules).
  system,

  /// Force Japanese.
  ja,

  /// Force English.
  en,
}
