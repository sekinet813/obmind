/// App display identity for About / license screens.
///
/// [versionName] is the pubspec `version` before `+`. Keep them aligned when
/// bumping the release.
abstract final class AppInfo {
  static const name = 'Obmind';
  static const versionName = '1.0.0';
}
